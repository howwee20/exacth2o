version: "2.4"

# Validation-only stub that lets the Balena Compose parser normalize the
# additive Gate B overlay without copying or rebuilding the real database
# service.
services:
  database_svc:
    image: walker-gate-b-parser-placeholder
