version: "2.4"

# Validation-only stub that lets the Balena Compose parser normalize the
# observer overlay without copying or rebuilding the real cron service.
services:
  cron_svc:
    image: walker-observer-parser-placeholder
